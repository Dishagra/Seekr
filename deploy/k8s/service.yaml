apiVersion: v1
kind: Service
metadata:
  name: seekr
  namespace: seekr
  labels:
    app.kubernetes.io/name: seekr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: seekr
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
